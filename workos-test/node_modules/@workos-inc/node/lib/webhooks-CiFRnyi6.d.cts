import { t as CryptoProvider } from "./crypto-provider-CYvkctEg.cjs";
import { ct as Event } from "./index-BkSnDb9g.cjs";

//#region src/webhooks/webhooks.d.ts
declare class Webhooks {
  private signatureProvider;
  constructor(cryptoProvider: CryptoProvider);
  get verifyHeader(): ({
    payload,
    sigHeader,
    secret,
    tolerance
  }: {
    payload: any;
    sigHeader: string;
    secret: string;
    tolerance?: number;
  }) => Promise<boolean>;
  get computeSignature(): (timestamp: any, payload: any, secret: string) => Promise<string>;
  get getTimestampAndSignatureHash(): (sigHeader: string) => [string, string];
  constructEvent({
    payload,
    sigHeader,
    secret,
    tolerance
  }: {
    payload: Record<string, unknown>;
    sigHeader: string;
    secret: string;
    tolerance?: number;
  }): Promise<Event>;
}
//#endregion
export { Webhooks as t };
//# sourceMappingURL=webhooks-CiFRnyi6.d.cts.map
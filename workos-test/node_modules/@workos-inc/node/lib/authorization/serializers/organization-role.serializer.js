//#region src/authorization/serializers/organization-role.serializer.ts
const deserializeRole = (role) => ({
	object: role.object,
	id: role.id,
	name: role.name,
	slug: role.slug,
	description: role.description,
	permissions: role.permissions,
	type: role.type,
	createdAt: role.created_at,
	updatedAt: role.updated_at
});
const deserializeOrganizationRole = (role) => ({
	object: role.object,
	id: role.id,
	name: role.name,
	slug: role.slug,
	description: role.description,
	permissions: role.permissions,
	type: "OrganizationRole",
	createdAt: role.created_at,
	updatedAt: role.updated_at
});

//#endregion
export { deserializeOrganizationRole, deserializeRole };
//# sourceMappingURL=organization-role.serializer.js.map

//#region src/directory-sync/serializers/directory.serializer.ts
const deserializeDirectory = (directory) => ({
	object: directory.object,
	id: directory.id,
	domain: directory.domain,
	externalKey: directory.external_key,
	name: directory.name,
	organizationId: directory.organization_id,
	state: deserializeDirectoryState(directory.state),
	type: directory.type,
	createdAt: directory.created_at,
	updatedAt: directory.updated_at
});
const deserializeDirectoryState = (state) => {
	if (state === "linked") return "active";
	if (state === "unlinked") return "inactive";
	return state;
};
const deserializeEventDirectory = (directory) => ({
	object: directory.object,
	id: directory.id,
	externalKey: directory.external_key,
	type: directory.type,
	state: directory.state,
	name: directory.name,
	organizationId: directory.organization_id,
	domains: directory.domains,
	createdAt: directory.created_at,
	updatedAt: directory.updated_at
});
const deserializeDeletedEventDirectory = (directory) => ({
	object: directory.object,
	id: directory.id,
	type: directory.type,
	state: directory.state,
	name: directory.name,
	organizationId: directory.organization_id,
	createdAt: directory.created_at,
	updatedAt: directory.updated_at
});

//#endregion
Object.defineProperty(exports, 'deserializeDeletedEventDirectory', {
  enumerable: true,
  get: function () {
    return deserializeDeletedEventDirectory;
  }
});
Object.defineProperty(exports, 'deserializeDirectory', {
  enumerable: true,
  get: function () {
    return deserializeDirectory;
  }
});
Object.defineProperty(exports, 'deserializeDirectoryState', {
  enumerable: true,
  get: function () {
    return deserializeDirectoryState;
  }
});
Object.defineProperty(exports, 'deserializeEventDirectory', {
  enumerable: true,
  get: function () {
    return deserializeEventDirectory;
  }
});
//# sourceMappingURL=directory.serializer-j1RV_J7z.cjs.map
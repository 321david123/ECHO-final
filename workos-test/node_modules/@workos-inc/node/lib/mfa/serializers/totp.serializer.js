//#region src/mfa/serializers/totp.serializer.ts
const deserializeTotp = (totp) => {
	return {
		issuer: totp.issuer,
		user: totp.user
	};
};
const deserializeTotpWithSecrets = (totp) => {
	return {
		issuer: totp.issuer,
		user: totp.user,
		qrCode: totp.qr_code,
		secret: totp.secret,
		uri: totp.uri
	};
};

//#endregion
export { deserializeTotp, deserializeTotpWithSecrets };
//# sourceMappingURL=totp.serializer.js.map
import { deserializeTotp, deserializeTotpWithSecrets } from "../../mfa/serializers/totp.serializer.js";

//#region src/user-management/serializers/factor.serializer.ts
const deserializeFactor = (factor) => ({
	object: factor.object,
	id: factor.id,
	createdAt: factor.created_at,
	updatedAt: factor.updated_at,
	type: factor.type,
	totp: deserializeTotp(factor.totp),
	userId: factor.user_id
});
const deserializeFactorWithSecrets = (factor) => ({
	object: factor.object,
	id: factor.id,
	createdAt: factor.created_at,
	updatedAt: factor.updated_at,
	type: factor.type,
	totp: deserializeTotpWithSecrets(factor.totp),
	userId: factor.user_id
});

//#endregion
export { deserializeFactor, deserializeFactorWithSecrets };
//# sourceMappingURL=factor.serializer.js.map
import { GeneratePortalLinkIntent } from "./interfaces/generate-portal-link-intent.interface.js";
import { WorkOS } from "../workos.js";

//#region src/portal/portal.d.ts
declare class Portal {
  private readonly workos;
  constructor(workos: WorkOS);
  generateLink({
    intent,
    organization,
    returnUrl,
    successUrl
  }: {
    intent: GeneratePortalLinkIntent;
    organization: string;
    returnUrl?: string;
    successUrl?: string;
  }): Promise<{
    link: string;
  }>;
}
//#endregion
export { Portal };
//# sourceMappingURL=portal.d.ts.map
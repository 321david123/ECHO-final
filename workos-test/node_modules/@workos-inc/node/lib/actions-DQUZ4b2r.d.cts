import { t as CryptoProvider } from "./crypto-provider-CYvkctEg.cjs";
import { t as ActionContext } from "./action.interface-B-xUqwBf.cjs";
import { n as ResponsePayload, r as UserRegistrationActionResponseData, t as AuthenticationActionResponseData } from "./response-payload.interface-B6BBFm56.cjs";

//#region src/actions/actions.d.ts
declare class Actions {
  private signatureProvider;
  constructor(cryptoProvider: CryptoProvider);
  private get computeSignature();
  get verifyHeader(): ({
    payload,
    sigHeader,
    secret,
    tolerance
  }: {
    payload: any;
    sigHeader: string;
    secret: string;
    tolerance?: number;
  }) => Promise<boolean>;
  serializeType(type: AuthenticationActionResponseData['type'] | UserRegistrationActionResponseData['type']): "authentication_action_response" | "user_registration_action_response";
  signResponse(data: AuthenticationActionResponseData | UserRegistrationActionResponseData, secret: string): Promise<{
    object: string;
    payload: ResponsePayload;
    signature: string;
  }>;
  constructAction({
    payload,
    sigHeader,
    secret,
    tolerance
  }: {
    payload: unknown;
    sigHeader: string;
    secret: string;
    tolerance?: number;
  }): Promise<ActionContext>;
}
//#endregion
export { Actions as t };
//# sourceMappingURL=actions-DQUZ4b2r.d.cts.map
const require_interface_check = require('./interface-check-jNs7TVqZ.cjs');

//#region src/fga/serializers/check-options.serializer.ts
const serializeCheckOptions = (options) => ({
	op: options.op,
	checks: options.checks.map(serializeCheckWarrantOptions),
	debug: options.debug
});
const serializeCheckBatchOptions = (options) => ({
	op: "batch",
	checks: options.checks.map(serializeCheckWarrantOptions),
	debug: options.debug
});
const serializeCheckWarrantOptions = (warrant) => {
	return {
		resource_type: require_interface_check.isResourceInterface(warrant.resource) ? warrant.resource.getResourceType() : warrant.resource.resourceType,
		resource_id: require_interface_check.isResourceInterface(warrant.resource) ? warrant.resource.getResourceId() : warrant.resource.resourceId ? warrant.resource.resourceId : "",
		relation: warrant.relation,
		subject: require_interface_check.isSubject(warrant.subject) ? {
			resource_type: warrant.subject.resourceType,
			resource_id: warrant.subject.resourceId
		} : {
			resource_type: warrant.subject.getResourceType(),
			resource_id: warrant.subject.getResourceId()
		},
		context: warrant.context ?? {}
	};
};
const deserializeDecisionTreeNode = (response) => {
	return {
		check: {
			resource: {
				resourceType: response.check.resource_type,
				resourceId: response.check.resource_id
			},
			relation: response.check.relation,
			subject: {
				resourceType: response.check.subject.resource_type,
				resourceId: response.check.subject.resource_id
			},
			context: response.check.context
		},
		policy: response.policy,
		decision: response.decision,
		processingTime: response.processing_time,
		children: response.children.map(deserializeDecisionTreeNode)
	};
};

//#endregion
Object.defineProperty(exports, 'deserializeDecisionTreeNode', {
  enumerable: true,
  get: function () {
    return deserializeDecisionTreeNode;
  }
});
Object.defineProperty(exports, 'serializeCheckBatchOptions', {
  enumerable: true,
  get: function () {
    return serializeCheckBatchOptions;
  }
});
Object.defineProperty(exports, 'serializeCheckOptions', {
  enumerable: true,
  get: function () {
    return serializeCheckOptions;
  }
});
//# sourceMappingURL=check-options.serializer-DlNVIJoZ.cjs.map
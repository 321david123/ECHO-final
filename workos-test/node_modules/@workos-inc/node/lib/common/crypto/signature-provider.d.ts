import { CryptoProvider } from "./crypto-provider.js";

//#region src/common/crypto/signature-provider.d.ts
declare class SignatureProvider {
  private cryptoProvider;
  constructor(cryptoProvider: CryptoProvider);
  verifyHeader({
    payload,
    sigHeader,
    secret,
    tolerance
  }: {
    payload: any;
    sigHeader: string;
    secret: string;
    tolerance?: number;
  }): Promise<boolean>;
  getTimestampAndSignatureHash(sigHeader: string): [string, string];
  computeSignature(timestamp: any, payload: any, secret: string): Promise<string>;
}
//#endregion
export { SignatureProvider };
//# sourceMappingURL=signature-provider.d.ts.map
import { deserializeApiKey } from "../../api-keys/serializers/api-key.serializer.js";
import { deserializeDirectoryGroup, deserializeUpdatedEventDirectoryGroup } from "../../directory-sync/serializers/directory-group.serializer.js";
import { deserializeDirectoryUser, deserializeUpdatedEventDirectoryUser } from "../../directory-sync/serializers/directory-user.serializer.js";
import { deserializeDeletedEventDirectory, deserializeEventDirectory } from "../../directory-sync/serializers/directory.serializer.js";
import { deserializeOrganizationDomain } from "../../organization-domains/serializers/organization-domain.serializer.js";
import { deserializeOrganization } from "../../organizations/serializers/organization.serializer.js";
import { deserializeConnection } from "../../sso/serializers/connection.serializer.js";
import { deserializeAuthenticationEvent } from "../../user-management/serializers/authentication-event.serializer.js";
import { deserializeUser } from "../../user-management/serializers/user.serializer.js";
import { deserializeEmailVerificationEvent } from "../../user-management/serializers/email-verification.serializer.js";
import { deserializeInvitationEvent } from "../../user-management/serializers/invitation.serializer.js";
import { deserializeMagicAuthEvent } from "../../user-management/serializers/magic-auth.serializer.js";
import { deserializePasswordResetEvent } from "../../user-management/serializers/password-reset.serializer.js";
import { deserializeSession } from "../../user-management/serializers/session.serializer.js";
import { deserializeOrganizationMembership } from "../../user-management/serializers/organization-membership.serializer.js";
import { deserializeRoleEvent } from "../../user-management/serializers/role.serializer.js";
import { deserializeAuthenticationRadarRiskDetectedEvent } from "../../user-management/serializers/authentication-radar-risk-event-serializer.js";
import { deserializeOrganizationRole } from "../../authorization/serializers/organization-role.serializer.js";
import { deserializePermission } from "../../authorization/serializers/permission.serializer.js";

//#region src/common/serializers/event.serializer.ts
const deserializeEvent = (event) => {
	const eventBase = {
		id: event.id,
		createdAt: event.created_at,
		context: event.context
	};
	switch (event.event) {
		case "authentication.email_verification_succeeded":
		case "authentication.magic_auth_failed":
		case "authentication.magic_auth_succeeded":
		case "authentication.mfa_succeeded":
		case "authentication.oauth_failed":
		case "authentication.oauth_succeeded":
		case "authentication.passkey_failed":
		case "authentication.passkey_succeeded":
		case "authentication.password_failed":
		case "authentication.password_succeeded":
		case "authentication.sso_failed":
		case "authentication.sso_succeeded": return {
			...eventBase,
			event: event.event,
			data: deserializeAuthenticationEvent(event.data)
		};
		case "authentication.radar_risk_detected": return {
			...eventBase,
			event: event.event,
			data: deserializeAuthenticationRadarRiskDetectedEvent(event.data)
		};
		case "connection.activated":
		case "connection.deactivated":
		case "connection.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeConnection(event.data)
		};
		case "dsync.activated": return {
			...eventBase,
			event: event.event,
			data: deserializeEventDirectory(event.data)
		};
		case "dsync.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeDeletedEventDirectory(event.data)
		};
		case "dsync.group.created":
		case "dsync.group.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeDirectoryGroup(event.data)
		};
		case "dsync.group.updated": return {
			...eventBase,
			event: event.event,
			data: deserializeUpdatedEventDirectoryGroup(event.data)
		};
		case "dsync.group.user_added":
		case "dsync.group.user_removed": return {
			...eventBase,
			event: event.event,
			data: {
				directoryId: event.data.directory_id,
				user: deserializeDirectoryUser(event.data.user),
				group: deserializeDirectoryGroup(event.data.group)
			}
		};
		case "dsync.user.created":
		case "dsync.user.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeDirectoryUser(event.data)
		};
		case "dsync.user.updated": return {
			...eventBase,
			event: event.event,
			data: deserializeUpdatedEventDirectoryUser(event.data)
		};
		case "email_verification.created": return {
			...eventBase,
			event: event.event,
			data: deserializeEmailVerificationEvent(event.data)
		};
		case "invitation.accepted":
		case "invitation.created":
		case "invitation.revoked":
		case "invitation.resent": return {
			...eventBase,
			event: event.event,
			data: deserializeInvitationEvent(event.data)
		};
		case "magic_auth.created": return {
			...eventBase,
			event: event.event,
			data: deserializeMagicAuthEvent(event.data)
		};
		case "password_reset.created":
		case "password_reset.succeeded": return {
			...eventBase,
			event: event.event,
			data: deserializePasswordResetEvent(event.data)
		};
		case "user.created":
		case "user.updated":
		case "user.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeUser(event.data)
		};
		case "organization_membership.created":
		case "organization_membership.deleted":
		case "organization_membership.updated": return {
			...eventBase,
			event: event.event,
			data: deserializeOrganizationMembership(event.data)
		};
		case "role.created":
		case "role.deleted":
		case "role.updated": return {
			...eventBase,
			event: event.event,
			data: deserializeRoleEvent(event.data)
		};
		case "organization_role.created":
		case "organization_role.updated":
		case "organization_role.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeOrganizationRole(event.data)
		};
		case "permission.created":
		case "permission.updated":
		case "permission.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializePermission(event.data)
		};
		case "session.created":
		case "session.revoked": return {
			...eventBase,
			event: event.event,
			data: deserializeSession(event.data)
		};
		case "organization.created":
		case "organization.updated":
		case "organization.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeOrganization(event.data)
		};
		case "organization_domain.verified":
		case "organization_domain.verification_failed":
		case "organization_domain.created":
		case "organization_domain.updated":
		case "organization_domain.deleted": return {
			...eventBase,
			event: event.event,
			data: deserializeOrganizationDomain(event.data)
		};
		case "api_key.created":
		case "api_key.revoked": return {
			...eventBase,
			event: event.event,
			data: deserializeApiKey(event.data)
		};
	}
};

//#endregion
export { deserializeEvent };
//# sourceMappingURL=event.serializer.js.map
import { RequestException } from "../interfaces/request-exception.interface.js";

//#region src/common/exceptions/not-found.exception.d.ts
declare class NotFoundException extends Error implements RequestException {
  readonly status = 404;
  readonly name = "NotFoundException";
  readonly message: string;
  readonly code?: string;
  readonly requestID: string;
  constructor({
    code,
    message,
    path,
    requestID
  }: {
    code?: string;
    message?: string;
    path: string;
    requestID: string;
  });
}
//#endregion
export { NotFoundException };
//# sourceMappingURL=not-found.exception.d.ts.map
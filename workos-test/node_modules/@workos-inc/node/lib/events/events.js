import { deserializeEvent } from "../common/serializers/event.serializer.js";
import { deserializeList } from "../common/serializers/list.serializer.js";
import { serializeListEventOptions } from "./serializers/list-event-options.serializer.js";

//#region src/events/events.ts
var Events = class {
	constructor(workos) {
		this.workos = workos;
	}
	async listEvents(options) {
		const { data } = await this.workos.get(`/events`, { query: options ? serializeListEventOptions(options) : void 0 });
		return deserializeList(data, deserializeEvent);
	}
};

//#endregion
export { Events };
//# sourceMappingURL=events.js.map
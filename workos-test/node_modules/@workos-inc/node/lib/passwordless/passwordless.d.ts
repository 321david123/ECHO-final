import { PasswordlessSession } from "./interfaces/passwordless-session.interface.js";
import { CreatePasswordlessSessionOptions } from "./interfaces/create-passwordless-session-options.interface.js";
import { SendSessionResponse } from "./interfaces/send-session-response.interface.js";
import { WorkOS } from "../workos.js";

//#region src/passwordless/passwordless.d.ts
declare class Passwordless {
  private readonly workos;
  constructor(workos: WorkOS);
  createSession({
    redirectURI,
    expiresIn,
    ...options
  }: CreatePasswordlessSessionOptions): Promise<PasswordlessSession>;
  sendSession(sessionId: string): Promise<SendSessionResponse>;
}
//#endregion
export { Passwordless };
//# sourceMappingURL=passwordless.d.ts.map